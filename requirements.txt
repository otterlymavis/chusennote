# FCM HTTP v1 authentication through Google Application Default Credentials.
google-auth[requests]>=2.35,<3

# Core tracking and SQLite functionality otherwise use only the Python standard
# library.

# Optional: headless-browser fetch for JavaScript-heavy ticket platforms.
# Enable at runtime with CHUSENNOTE_BROWSER_FETCH=fallback (or "always").
# Install with:
#   pip install playwright
#   playwright install chromium
# playwright>=1.40

# Optional: Postgres backend (hosted deployments). SQLite needs no driver.
# Enable by setting CHUSENNOTE_DATABASE_URL=postgresql://...
# psycopg[binary]>=3.2
